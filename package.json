{
  "name": "mission-control",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler pages deploy ./ --project-name mission-control"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
